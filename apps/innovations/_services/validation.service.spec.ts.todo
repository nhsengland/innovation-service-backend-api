describe('Innovations / _services / validation suite', () => {
  // let sut: ValidationService;

  // // No tests do yet since this depends on history table
  // describe.skip('checkEngagingSupport', () = {
  // });

  describe.skip('validate', () => {
    it.skip("implement test when we have a check that doesn't depend on history table", () => {
      fail('Implement test');
    });
  });
});
